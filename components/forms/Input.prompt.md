**Input** — labelled text field for booking & contact forms.

```jsx
<Input label="Nombre" placeholder="Tu nombre" />
<Input label="Email" type="email" error="Introduce un email válido" />
```

Optional `icon` (leading), `hint` (helper text), `error` (message + error border). Label renders in tracked uppercase.
