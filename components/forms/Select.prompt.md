**Select** — styled dropdown for service, staff, and time-slot pickers. Matches `Input`.

```jsx
<Select label="Servicio" placeholder="Elige un servicio"
  options={["Semipermanente color", "Uñas de gel", "Pedicura exprés"]} />
```

Accepts an array of strings or `{value, label}` objects.
