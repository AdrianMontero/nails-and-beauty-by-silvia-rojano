**Rating** — gold star row for reviews and the salon's 5.0 score.

```jsx
<Rating value={5} showValue count={56} />
<Rating value={4.5} size={14} />
```

`value` accepts halves; `count` appends "{n} reseñas"; `showValue` prints the number.
